# Demo repos

`analytics-helper/` is what the *PR diff demo* workflow watches. It is a copy of
`examples/repos/mcp-promise-kept` kept apart from the test fixtures so a demo
PR can widen it without touching the tests. The open demo PR does exactly that.
